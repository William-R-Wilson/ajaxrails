<%= link_to "Assign Roles", assign_roles_path %>
<%= link_to "New User", new_user_path %>
<h1>All Users<h2>
<% @users.each do |u| %>
  <div><%= link_to u.name, user_path(u) %>, <%= u.role %></div>
<% end %>
